import 'package:flutter/material.dart';

const primaryColor = Color(0xFFF6576D);
const bodyTextColor = Color(0xFF92898B);
const bgColor = Color(0xFF1E1E28);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1);
const maxWidth = 1440.0;

const descriptionText = "Free unlimited consultents with our panel doctor up to 15% discount on home diagnostic sample collection \$15000 discount on helth & life insurance";